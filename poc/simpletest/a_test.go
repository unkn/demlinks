package demlinks

import (
	// "fmt"
	// "os"
	// "os/exec"
	// "path/filepath"
	"testing"
	// "time"
)

func TestNothing(t *testing.T) {
	/*
		temp := os.Getenv("TEMP")
		what := filepath.Join(temp, "a.exe")
		c := exec.Command(what)
		c.Run()
		time.Sleep(1000 * time.Millisecond)

		//os.OpenFile(what, flag, perm)

		fmt.Println(os.Remove(what))*/
	t.FailNow()
}
